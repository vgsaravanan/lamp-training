<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}User Management System{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets '@UserBundle/Resources/public/css/*' %}
                <link rel="stylesheet" href="{{ asset_url }}" />
                {% endstylesheets %}
        {% endblock %}
    </head>
    <body>
        <div>
            <div class="header">
                    {% image '@UserBundle/Resources/public/image/*' %}
                    <img src="{{ asset_url }}" alt="Management System" />
                    {% endimage %}
                    <h1 class="headerlink" id="head">
                        <a href="{{ path('list_user') }}">USER MANAGEMENT SYSTEM</a>
                    </h1>
            </div>

            <div class="navigation">
                    <ul class="navbar">
                        <li><a href="{{ path('list_user') }}">Home</a></li>
                        <li><a href="{{ path('new_user') }}">New User</a></li>
                        <li><a href="{{ path('list_user') }}">List User</a></li>

                        <li style="float: right;"><a href="">Log out</a></li>   
                     </ul>
            </div>
        </div>
        <div id="block_content">
           
        </div>
    </body>
</html>